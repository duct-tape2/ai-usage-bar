---
name: Wrong number
about: The dashboard shows a different remaining amount than the vendor
title: "[provider] dashboard says X, vendor says Y"
labels: bug
---

**Provider**: codex / claude-code / cursor / chatgpt-pro

**Dashboard showed**:

**Vendor's own UI showed** (screenshot or text):

**`/api/usage` for that meter** (remove ids and tokens):

```json
```

**Commit** (`git rev-parse --short HEAD`):

**OS / Node**:
